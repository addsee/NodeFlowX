import 'package:flutter/material.dart';

class AppColors {
  // Dark Theme
  static const Color background = Color(0xFF0F0F1A);
  static const Color surface = Color(0xFF1A1A2E);
  static const Color glass = Color(0x33FFFFFF);

  // Light Theme
  static const Color lightBackground = Color(0xFFF0F2F5);
  static const Color lightSurface = Color(0xFFFFFFFF);
  static const Color lightGlass = Color(0x33000000);

  // Accents (Neon for dark, slightly softer for light if needed, but keeping neon for brand identity)
  static const Color neonCyan = Color(0xFF00FBFF);
  static const Color neonMagenta = Color(0xFFE600FF);
  static const Color neonAmber = Color(0xFFFFB300);

  static const Color lightCyan = Color(0xFF00B8D4);
  static const Color lightMagenta = Color(0xFFD500F9);

  // Text
  static const Color text = Colors.white;
  static const Color textDim = Colors.white70;
  
  static const Color lightText = Color(0xFF1E1E1E);
  static const Color lightTextDim = Color(0xFF616161);
}
